<UserControl x:Class="Project212.PoliceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project212"
        mc:Ignorable="d"
        Background="White"
        Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Danh Sách Phương Tiện" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,10,0,10"/>

        <DataGrid x:Name="RecordDataGrid" Grid.Row="1" AutoGenerateColumns="False" SelectionChanged="RecordDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Vehicle" Binding="{Binding Vehicle.Plate}" Width="80"/>
                <DataGridTextColumn Header="Kết quả" Binding="{Binding ResultText}" Width="100">
                </DataGridTextColumn>
                <DataGridTextColumn Header="CO (g/km)" Binding="{Binding Co}" Width="80"/>
                <DataGridTextColumn Header="HC (g/km)" Binding="{Binding Hc}" Width="80"/>
                <DataGridTextColumn Header="NOx (g/km)" Binding="{Binding Nox}" Width="80"/>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding Note}" Width="80"/>
                <DataGridTextColumn Header="Thời gian " Binding="{Binding Time.InspectTime}" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <TextBlock Text="Chi Tiết Phương Tiện" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>

            <Grid Margin="0,5,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Cột 1: ID -->
                <TextBlock Text="ID:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="txtID" Grid.Row="0" Grid.Column="1" IsReadOnly="True" Margin="5"/>

                <!-- Cột 2: Biển Số -->
                <TextBlock Text="Biển Số:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="txtLicensePlate" Grid.Row="0" Grid.Column="3" Margin="5" />

                <!-- Cột 1: Loại Xe -->
                <TextBlock Text="Loại Xe:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="txtVehicleType" Grid.Row="1" Grid.Column="1" Margin="5" />

                <!-- Cột 2: Chủ Sở Hữu -->
                <TextBlock Text="Chủ Sở Hữu:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="txtOwner" Grid.Row="1" Grid.Column="3" Margin="5" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
